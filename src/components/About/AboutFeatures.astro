---
import { Image } from "astro:assets";
import Card from "../Card";
import features from "../../data/features.json";
import check from "../../assets/images/check.svg";
import camera from '../../assets/images/camera.svg';
import money from '../../assets/images/money.svg';
const featuresArr = features.data;

const getImageSrc = (id: number) => {
  switch (id) {
    case 1:
      return check;
    case 2:
      return camera;
    default:
      return money;
  }
};
---
<div class="flex justify-center items-center mt-8 lg:flex-nowrap flex-wrap">
  {featuresArr.map(feature => (
    <Card key={feature.id}>
      <Image src={getImageSrc(feature.id)} alt="feature image" class="m-auto h-[48px] w-[48px]"/>
      <h3 class="font-black text-4xl text-center">{feature.title}</h3>
      <p class="mt-4">{feature.description}</p>
    </Card>
  ))}
</div>